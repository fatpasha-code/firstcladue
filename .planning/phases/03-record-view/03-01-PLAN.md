---
phase: 03-record-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/tabs.tsx
  - src/components/ui/collapsible.tsx
  - src/components/ui/skeleton.tsx
  - src/components/ui/badge.tsx
  - src/app/records/[id]/page.tsx
  - src/app/records/[id]/loading.tsx
  - src/app/records/[id]/not-found.tsx
  - src/components/record/record-header.tsx
  - src/components/record/extraction-tab.tsx
  - src/components/record/interpretation-tab.tsx
  - src/components/record/evidence-collapsible.tsx
  - src/components/record/impact-badge.tsx
  - src/components/record/inferred-badge.tsx
autonomous: true
requirements:
  - EXTRACT-03
  - INTERP-02
  - INTERP-03

must_haves:
  truths:
    - "User navigates to /records/[id] and sees the record header with date, label, and StatusBadge"
    - "User sees 6 tabs in order: Сделано, В работе, Блокеры, Назначения, Дедлайны, Интерпретация"
    - "User sees extracted data rendered correctly per tab (items, impact badges, inferred badges, assignment grouping)"
    - "User sees interpretation tab with all blocks in correct order (D-08)"
    - "Empty tabs show 'Ничего не найдено' — never hidden"
    - "real_status is visible as colored StatusBadge in header and interpretation tab"
    - "Inferred deadlines are visually distinguished with 'выведен' badge"
  artifacts:
    - path: "src/app/records/[id]/page.tsx"
      provides: "Record page Server Component"
      min_lines: 20
    - path: "src/components/record/record-header.tsx"
      provides: "Page header with date, label, StatusBadge, back link"
      min_lines: 15
    - path: "src/components/record/extraction-tab.tsx"
      provides: "Renders extraction data for all 5 extraction tabs"
      min_lines: 80
    - path: "src/components/record/interpretation-tab.tsx"
      provides: "Renders interpretation blocks in D-08 order"
      min_lines: 60
    - path: "src/components/record/evidence-collapsible.tsx"
      provides: "Claim + expandable evidence"
      min_lines: 20
  key_links:
    - from: "src/app/records/[id]/page.tsx"
      to: "supabase.from('records')"
      via: "Server Component fetch"
      pattern: "supabase.*from.*records.*select"
    - from: "src/app/records/[id]/page.tsx"
      to: "src/components/record/record-header.tsx"
      via: "import and render"
      pattern: "RecordHeader"
    - from: "src/components/record/extraction-tab.tsx"
      to: "src/lib/ai/schemas.ts"
      via: "type imports"
      pattern: "import.*ExtractedData.*from.*schemas"
---

<objective>
Build the read-only record view page at `/records/[id]` with 6 structured tabs displaying extraction and interpretation data.

Purpose: Users need to view analysis results (EXTRACT-03, INTERP-02, INTERP-03). This is the primary consumption interface for extracted and interpreted data.
Output: Working `/records/[id]` page with header, 6 tabs, all extraction categories rendered with domain-specific formatting, interpretation tab with evidence collapsibles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-record-view/03-CONTEXT.md
@.planning/phases/03-record-view/03-RESEARCH.md
@.planning/phases/03-record-view/03-UI-SPEC.md

@src/lib/ai/schemas.ts
@src/app/actions.ts
@src/components/status-badge.tsx
@src/app/page.tsx
@src/lib/supabase/server.ts

<interfaces>
<!-- Key types and contracts from the codebase. Executor uses these directly. -->

From src/lib/ai/schemas.ts:
```typescript
export const ExtractedDataSchema = z.object({
  done: z.array(z.object({ description: z.string(), person: z.string().optional() })),
  in_progress: z.array(z.object({ description: z.string(), person: z.string().optional(), deadline: z.string().optional() })),
  blockers: z.array(z.object({ description: z.string(), impact: z.enum(['high', 'medium', 'low']) })),
  assignments: z.array(z.object({ person: z.string(), tasks: z.array(z.string()), by_when: z.string().optional() })),
  deadlines: z.array(z.object({ date: z.string(), description: z.string(), type: z.enum(['explicit', 'inferred']), condition: z.string().optional() }))
})

export const EvidenceSchema = z.object({ type: z.enum(['quote', 'paraphrase']), text: z.string().min(1), speaker: z.string().optional(), confidence: z.enum(['high', 'medium']) })
export const GroundedClaimSchema = z.object({ claim: z.string(), evidence: EvidenceSchema })
export const InterpretationSchema = z.object({
  summary: z.string(), management_view: z.string(),
  hidden_blockers: z.array(GroundedClaimSchema), ambiguities: z.array(GroundedClaimSchema),
  clarification_questions: z.array(z.string()),
  real_status: z.enum(['green', 'yellow', 'red']), real_status_reason: z.string()
})

export type ExtractedData = z.infer<typeof ExtractedDataSchema>
export type Interpretation = z.infer<typeof InterpretationSchema>
export type GroundedClaim = z.infer<typeof GroundedClaimSchema>
```

From src/components/status-badge.tsx:
```typescript
export function StatusBadge({ status }: { status: 'green' | 'yellow' | 'red' })
// Uses: bg-emerald-100 text-emerald-800 (green), bg-amber-100 text-amber-800 (yellow), bg-red-100 text-red-800 (red)
// Labels: 'В порядке' | 'Есть вопросы' | 'Требует внимания'
```

From src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>
```

Server Component pattern (from src/app/page.tsx):
```typescript
const supabase = await createClient()
const { data: { user } } = await supabase.auth.getUser()
if (!user) redirect('/login')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn components and create page route scaffold</name>
  <read_first>
    - src/components/ui/button.tsx (existing shadcn component pattern)
    - src/app/page.tsx (existing Server Component page pattern)
    - src/lib/supabase/server.ts (Supabase client usage)
    - .planning/phases/03-record-view/03-UI-SPEC.md (component inventory, install commands)
  </read_first>
  <files>
    src/components/ui/tabs.tsx
    src/components/ui/collapsible.tsx
    src/components/ui/skeleton.tsx
    src/components/ui/badge.tsx
    src/app/records/[id]/page.tsx
    src/app/records/[id]/loading.tsx
    src/app/records/[id]/not-found.tsx
    src/components/record/record-header.tsx
    src/components/record/impact-badge.tsx
    src/components/record/inferred-badge.tsx
  </files>
  <action>
    **Step 1: Install 4 shadcn components:**
    ```bash
    npx shadcn@latest add tabs
    npx shadcn@latest add collapsible
    npx shadcn@latest add skeleton
    npx shadcn@latest add badge
    ```
    These generate files into `src/components/ui/`.

    **Step 2: Create `src/app/records/[id]/page.tsx`** — async Server Component:
    - Import `createClient` from `@/lib/supabase/server`
    - Import `notFound, redirect` from `next/navigation`
    - Destructure params: `const { id } = await params` (Next.js 16 — params is Promise)
    - Auth check: `const { data: { user } } = await supabase.auth.getUser()` → if !user, `redirect('/login')`
    - Fetch record: `supabase.from('records').select('id, label, status, extracted_data, user_corrections, interpretation, created_at').eq('id', id).single()`
    - If error or !record, call `notFound()`
    - Parse JSONB: `const displayData = ExtractedDataSchema.parse(record.user_corrections ?? record.extracted_data)` wrapped in try/catch — on parse failure show error state
    - Parse interpretation: `const interpretation = record.interpretation ? InterpretationSchema.parse(record.interpretation) : null` — also try/catch
    - Render: `<div className="mx-auto max-w-4xl px-8 py-8">` containing `<RecordHeader>` and a placeholder for tabs (will be wired in Task 2)
    - Page max-width: `max-w-4xl` (896px), centered `mx-auto`, padding `px-8 py-8`

    **Step 3: Create `src/app/records/[id]/loading.tsx`:**
    - Use Skeleton component. Show: one line skeleton for back link, one block for header area, horizontal bar for tab row, card-sized skeleton for content.
    - Pattern: `<div className="mx-auto max-w-4xl px-8 py-8"><Skeleton className="h-4 w-16 mb-6" />...`

    **Step 4: Create `src/app/records/[id]/not-found.tsx`:**
    - Import Alert from `@/components/ui/alert`
    - Import Link from `next/link`
    - Show: `<Link href="/" className="text-muted-foreground hover:text-foreground text-sm">Назад</Link>`
    - Alert (non-destructive): "Запись не найдена."

    **Step 5: Create `src/components/record/record-header.tsx`** — Server Component (no 'use client'):
    - Props: `{ record: { label: string | null; created_at: string; interpretation: { real_status: 'green' | 'yellow' | 'red' } | null } }`
    - Back link: `<Link href="/" className="text-muted-foreground hover:text-foreground text-sm mb-6 inline-block">Назад</Link>` (per D-01)
    - Title line: `<h1 className="text-xl font-semibold leading-[1.2]">` — if label exists, show label; else show "Запись от {formatted date}" (per UI-SPEC copywriting)
    - Date formatting: `new Intl.DateTimeFormat('ru-RU', { day: 'numeric', month: 'long', year: 'numeric' }).format(new Date(record.created_at))` — e.g. "29 марта 2026"
    - StatusBadge: render to the right of title if interpretation exists. Use `<StatusBadge status={record.interpretation.real_status} />`
    - If label exists, show label as title and formatted date as muted text below
    - Layout: title + badge on same line using flex

    **Step 6: Create `src/components/record/impact-badge.tsx`** (per D-05):
    - Props: `{ impact: 'high' | 'medium' | 'low' }`
    - Color map: `high: 'bg-red-100 text-red-800 dark:bg-red-900/30 dark:text-red-300'`, `medium: 'bg-amber-100 text-amber-800 dark:bg-amber-900/30 dark:text-amber-300'`, `low: 'bg-neutral-100 text-neutral-600 dark:bg-neutral-800 dark:text-neutral-400'`
    - Labels: `{ high: 'высокий', medium: 'средний', low: 'низкий' }`
    - Include `<span className="sr-only">Влияние: </span>` before label text (accessibility per UI-SPEC)
    - Use: `<span className="inline-flex items-center rounded-full px-2 py-0.5 text-xs font-medium {colorClass}">`

    **Step 7: Create `src/components/record/inferred-badge.tsx`** (per D-06):
    - No props needed (always "выведен")
    - Classes: `bg-neutral-100 text-neutral-600 dark:bg-neutral-800 dark:text-neutral-400`
    - Render: `<span className="inline-flex items-center rounded-full px-2 py-0.5 text-xs font-medium {classes}">выведен</span>`
  </action>
  <verify>
    <automated>cd C:/firstcladue && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/tabs.tsx exists and contains "TabsTrigger"
    - src/components/ui/collapsible.tsx exists and contains "CollapsibleContent"
    - src/components/ui/skeleton.tsx exists and contains "Skeleton"
    - src/components/ui/badge.tsx exists and contains "Badge"
    - src/app/records/[id]/page.tsx contains "await params" and "notFound()" and "ExtractedDataSchema.parse"
    - src/app/records/[id]/page.tsx contains `.select('id, label, status, extracted_data, user_corrections, interpretation, created_at')`
    - src/app/records/[id]/page.tsx does NOT contain "raw_text" in the select query (per D-02)
    - src/app/records/[id]/loading.tsx contains "Skeleton"
    - src/app/records/[id]/not-found.tsx contains "Запись не найдена"
    - src/components/record/record-header.tsx contains 'Назад' and 'StatusBadge' and 'Intl.DateTimeFormat'
    - src/components/record/record-header.tsx does NOT contain "'use client'"
    - src/components/record/impact-badge.tsx contains "bg-red-100" and "bg-amber-100" and "bg-neutral-100" and "sr-only"
    - src/components/record/inferred-badge.tsx contains "выведен"
    - TypeScript compiles without errors (npx tsc --noEmit exits 0)
  </acceptance_criteria>
  <done>Page route exists at /records/[id], fetches record from Supabase, renders header with date/label/StatusBadge/back-link, shows loading and not-found states. Domain badges (impact, inferred) ready for use.</done>
</task>

<task type="auto">
  <name>Task 2: Build extraction tabs with domain-specific rendering</name>
  <read_first>
    - src/app/records/[id]/page.tsx (page scaffold from Task 1)
    - src/lib/ai/schemas.ts (ExtractedData type — exact field names)
    - src/components/record/impact-badge.tsx (created in Task 1)
    - src/components/record/inferred-badge.tsx (created in Task 1)
    - .planning/phases/03-record-view/03-UI-SPEC.md (extraction tab details section, copywriting contract)
    - .planning/phases/03-record-view/03-CONTEXT.md (D-03 through D-07)
  </read_first>
  <files>
    src/components/record/extraction-tab.tsx
    src/app/records/[id]/page.tsx
  </files>
  <action>
    **Step 1: Create `src/components/record/extraction-tab.tsx`** — Client Component ('use client'):

    This single file renders all 5 extraction tab contents based on a `tab` prop. It does NOT manage tabs itself — it receives which tab to render.

    Props:
    ```typescript
    type ExtractionTabProps = {
      tab: 'done' | 'in_progress' | 'blockers' | 'assignments' | 'deadlines'
      data: ExtractedData
    }
    ```

    Import: `ExtractedData` type from `@/lib/ai/schemas`, `ImpactBadge` from `./impact-badge`, `InferredBadge` from `./inferred-badge`, `Calendar` icon from `lucide-react`.

    **Empty state** (per D-04): For every tab, if `data[tab]` is empty array (`.length === 0`), render:
    `<p className="text-muted-foreground text-sm py-8 text-center">Ничего не найдено</p>`

    **Tab: done** — Map `data.done[]`. Each item: `description` as `<p className="text-sm leading-relaxed">`. If `person` present: `<span className="text-muted-foreground text-xs ml-2">{person}</span>` after description.

    **Tab: in_progress** — Map `data.in_progress[]`. Each item: `description` as body text. If `person`: muted small text. If `deadline`: muted small text with Calendar icon (14px, `className="inline h-3.5 w-3.5 mr-1"`).

    **Tab: blockers** (per D-05) — Map `data.blockers[]`. Each item on one line: `<ImpactBadge impact={item.impact} />` followed by `description` as body text. Badge appears before description with `mr-2` gap.

    **Tab: assignments** (per D-07) — Map `data.assignments[]`. Group by person: `<h3 className="text-sm font-semibold leading-[1.4]">{person}</h3>`, below it `<ul>` with `tasks[]` as `<li className="text-sm leading-relaxed">`. If `by_when` present: `<p className="text-muted-foreground text-xs mt-1">{by_when}</p>` below task list.

    **Tab: deadlines** (per D-06) — Map `data.deadlines[]`. Each item: `<span className="text-sm font-semibold">{date}</span>` + if `type === 'inferred'`: `<InferredBadge />` with `ml-2`. Then `description` as body text. If `condition`: `<p className="text-muted-foreground text-xs mt-1">{condition}</p>`.

    Each item in any list should be wrapped in a container with `py-3 border-b last:border-b-0` for visual separation.

    **Step 2: Update `src/app/records/[id]/page.tsx`** to wire tabs:

    Import `Tabs, TabsList, TabsTrigger, TabsContent` from `@/components/ui/tabs`.
    Import `Card, CardContent` from `@/components/ui/card`.
    Import `ExtractionTab` from `@/components/record/extraction-tab`.

    Below `<RecordHeader>`, add 48px gap (`mt-12`), then render:
    ```tsx
    <Tabs defaultValue="done" className="w-full">
      <TabsList>
        <TabsTrigger value="done">Сделано</TabsTrigger>
        <TabsTrigger value="in_progress">В работе</TabsTrigger>
        <TabsTrigger value="blockers">Блокеры</TabsTrigger>
        <TabsTrigger value="assignments">Назначения</TabsTrigger>
        <TabsTrigger value="deadlines">Дедлайны</TabsTrigger>
        <TabsTrigger value="interpretation">Интерпретация</TabsTrigger>
      </TabsList>
      <Card className="mt-4">
        <CardContent className="p-6">
          <TabsContent value="done" className="mt-0">
            <ExtractionTab tab="done" data={displayData} />
          </TabsContent>
          <TabsContent value="in_progress" className="mt-0">
            <ExtractionTab tab="in_progress" data={displayData} />
          </TabsContent>
          <TabsContent value="blockers" className="mt-0">
            <ExtractionTab tab="blockers" data={displayData} />
          </TabsContent>
          <TabsContent value="assignments" className="mt-0">
            <ExtractionTab tab="assignments" data={displayData} />
          </TabsContent>
          <TabsContent value="deadlines" className="mt-0">
            <ExtractionTab tab="deadlines" data={displayData} />
          </TabsContent>
          <TabsContent value="interpretation" className="mt-0">
            {/* InterpretationTab placeholder — wired in Task 3 */}
          </TabsContent>
        </CardContent>
      </Card>
    </Tabs>
    ```

    Tab labels per D-03: Сделано, В работе, Блокеры, Назначения, Дедлайны, Интерпретация.
    Tab values: done, in_progress, blockers, assignments, deadlines, interpretation.
  </action>
  <verify>
    <automated>cd C:/firstcladue && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/record/extraction-tab.tsx contains "'use client'"
    - src/components/record/extraction-tab.tsx contains "Ничего не найдено" (D-04 empty state)
    - src/components/record/extraction-tab.tsx contains "ImpactBadge" (D-05)
    - src/components/record/extraction-tab.tsx contains "InferredBadge" (D-06)
    - src/components/record/extraction-tab.tsx contains "Calendar" from lucide-react
    - src/app/records/[id]/page.tsx contains "TabsTrigger" and "Сделано" and "В работе" and "Блокеры" and "Назначения" and "Дедлайны" and "Интерпретация"
    - src/app/records/[id]/page.tsx contains "ExtractionTab"
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All 5 extraction tabs render correct data with domain-specific formatting. Empty tabs show "Ничего не найдено". Blocker impact shown as colored badges. Inferred deadlines marked with "выведен" badge. Assignments grouped by person.</done>
</task>

<task type="auto">
  <name>Task 3: Build interpretation tab with evidence collapsibles</name>
  <read_first>
    - src/lib/ai/schemas.ts (InterpretationSchema, GroundedClaimSchema, EvidenceSchema)
    - src/components/status-badge.tsx (StatusBadge component)
    - src/app/records/[id]/page.tsx (page with tabs from Task 2)
    - .planning/phases/03-record-view/03-UI-SPEC.md (interpretation tab layout section, D-08, D-09)
    - .planning/phases/03-record-view/03-CONTEXT.md (D-08, D-09)
  </read_first>
  <files>
    src/components/record/interpretation-tab.tsx
    src/components/record/evidence-collapsible.tsx
    src/app/records/[id]/page.tsx
  </files>
  <action>
    **Step 1: Create `src/components/record/evidence-collapsible.tsx`** — Client Component ('use client'):

    Per D-09: claim visible, evidence collapsed behind "источник" link.

    Props: `{ claim: GroundedClaim }` (import `GroundedClaim` type from `@/lib/ai/schemas`).

    Import `Collapsible, CollapsibleTrigger, CollapsibleContent` from `@/components/ui/collapsible`, `ChevronDown` from `lucide-react`.

    Structure:
    ```tsx
    <div className="py-3 border-b last:border-b-0">
      <p className="text-sm leading-relaxed">{claim.claim}</p>
      <Collapsible>
        <CollapsibleTrigger className="text-muted-foreground text-xs hover:text-foreground flex items-center gap-1 mt-1">
          источник
          <ChevronDown className="h-3 w-3 transition-transform duration-200 [[data-state=open]_&]:rotate-180" />
        </CollapsibleTrigger>
        <CollapsibleContent className="mt-2">
          <p className="text-sm italic text-muted-foreground">{claim.evidence.text}</p>
          {claim.evidence.speaker && (
            <p className="text-xs text-muted-foreground mt-1">{claim.evidence.speaker}</p>
          )}
        </CollapsibleContent>
      </Collapsible>
    </div>
    ```

    Note: `claim.evidence.speaker` is optional per EvidenceSchema — render conditionally.

    **Step 2: Create `src/components/record/interpretation-tab.tsx`** — Client Component ('use client'):

    Props: `{ interpretation: Interpretation | null }` (import `Interpretation` type from `@/lib/ai/schemas`).

    If `interpretation` is null: render empty state `<p className="text-muted-foreground text-sm py-8 text-center">Интерпретация недоступна</p>`.

    Import `StatusBadge` from `@/components/status-badge`, `EvidenceCollapsible` from `./evidence-collapsible`.

    Render blocks in exact D-08 order with 24px (`space-y-6`) vertical gap between sections. Each section heading: `<h2 className="text-xl font-semibold leading-[1.2]">`.

    1. **Статус** — `<h2>Статус</h2>` + `<StatusBadge status={interpretation.real_status} />` + `<p className="text-sm leading-relaxed mt-2">{interpretation.real_status_reason}</p>`
    2. **Резюме** — `<h2>Резюме</h2>` + `<p className="text-sm leading-relaxed">{interpretation.summary}</p>`
    3. **Управленческий взгляд** — `<h2>Управленческий взгляд</h2>` + `<p className="text-sm leading-relaxed">{interpretation.management_view}</p>`
    4. **Скрытые блокеры** — `<h2>Скрытые блокеры</h2>`. If `hidden_blockers.length === 0`: empty state "Ничего не найдено". Else: map to `<EvidenceCollapsible claim={item} />`.
    5. **Неясности** — `<h2>Неясности</h2>`. If `ambiguities.length === 0`: empty state. Else: map to `<EvidenceCollapsible claim={item} />`.
    6. **Что уточнить** — `<h2>Что уточнить</h2>`. If `clarification_questions.length === 0`: empty state. Else: `<ol className="list-decimal list-inside space-y-2">` with each question as `<li className="text-sm leading-relaxed">`.

    **Step 3: Update `src/app/records/[id]/page.tsx`:**

    Import `InterpretationTab` from `@/components/record/interpretation-tab`.

    Replace the interpretation TabsContent placeholder with:
    ```tsx
    <TabsContent value="interpretation" className="mt-0">
      <InterpretationTab interpretation={interpretation} />
    </TabsContent>
    ```

    Where `interpretation` is the parsed variable from the page component (already exists from Task 1).
  </action>
  <verify>
    <automated>cd C:/firstcladue && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/record/evidence-collapsible.tsx contains "'use client'" and "CollapsibleTrigger" and "источник" and "claim.evidence.speaker"
    - src/components/record/interpretation-tab.tsx contains "'use client'" and "StatusBadge" and "EvidenceCollapsible"
    - src/components/record/interpretation-tab.tsx contains section headings in order: "Статус" before "Резюме" before "Управленческий взгляд" before "Скрытые блокеры" before "Неясности" before "Что уточнить"
    - src/components/record/interpretation-tab.tsx contains "Ничего не найдено" for empty arrays
    - src/components/record/interpretation-tab.tsx contains "real_status_reason"
    - src/app/records/[id]/page.tsx contains "InterpretationTab" and "interpretation={interpretation}"
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Interpretation tab renders all 6 blocks in D-08 order. StatusBadge shows real_status with reason text below. Hidden blockers and ambiguities use EvidenceCollapsible with expandable evidence. Clarification questions render as numbered list. Empty arrays show "Ничего не найдено".</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — all types correct
2. `npm run build` succeeds — no build errors
3. Navigate to `/records/[valid-id]` — page loads with header, tabs, data
4. Navigate to `/records/[invalid-id]` — shows not-found page
5. All 6 tabs visible and clickable
6. Empty tabs show "Ничего не найдено"
7. Blocker impact badges show correct colors (red/amber/neutral)
8. Inferred deadlines show "выведен" badge
9. Interpretation tab shows all blocks in correct order
10. Evidence sections expand/collapse on "источник" click
</verification>

<success_criteria>
- /records/[id] loads record from Supabase and renders structured view
- 6 tabs visible in order: Сделано, В работе, Блокеры, Назначения, Дедлайны, Интерпретация
- Extraction data rendered with domain formatting (impact badges, inferred badges, assignment grouping)
- Interpretation tab shows real_status + reason, summary, management_view, hidden_blockers, ambiguities, clarification_questions
- Evidence collapsible works (click "источник" to expand)
- Empty state "Ничего не найдено" for empty tabs/sections
- user_corrections takes priority over extracted_data when present
- TypeScript compiles, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-record-view/03-01-SUMMARY.md`
</output>
