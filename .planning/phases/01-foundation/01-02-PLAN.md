---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/login/login-form.tsx
  - src/app/(auth)/login/actions.ts
  - src/app/(auth)/reset-password/page.tsx
  - src/app/(auth)/reset-password/reset-password-form.tsx
  - src/app/(auth)/reset-password/actions.ts
  - src/app/auth/confirm/route.ts
  - src/app/page.tsx
  - src/app/actions.ts
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "User can sign in with email and password on /login page"
    - "Failed login shows inline error message below the form"
    - "Successful login redirects to /"
    - "User can click Forgot password? and receive reset email"
    - "User can set new password after clicking reset email link"
    - "User can sign out from the home page"
    - "Session persists across browser refresh (cookie-based)"
  artifacts:
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Login page (Server Component wrapper)"
      min_lines: 5
    - path: "src/app/(auth)/login/login-form.tsx"
      provides: "Login form with email/password fields, inline error, forgot password link"
      contains: "use client"
    - path: "src/app/(auth)/login/actions.ts"
      provides: "Server Actions for login and password reset request"
      contains: "use server"
      exports: ["login", "resetPassword"]
    - path: "src/app/(auth)/reset-password/page.tsx"
      provides: "Password reset page"
      min_lines: 5
    - path: "src/app/(auth)/reset-password/actions.ts"
      provides: "Server Action for updating password"
      contains: "use server"
      exports: ["updatePassword"]
    - path: "src/app/auth/confirm/route.ts"
      provides: "Email confirmation/reset link handler"
      exports: ["GET"]
    - path: "src/app/actions.ts"
      provides: "Global Server Actions including sign out"
      exports: ["signOut"]
  key_links:
    - from: "src/app/(auth)/login/login-form.tsx"
      to: "src/app/(auth)/login/actions.ts"
      via: "import login, resetPassword"
      pattern: "import.*login.*from.*actions"
    - from: "src/app/(auth)/login/actions.ts"
      to: "src/lib/supabase/server.ts"
      via: "import createClient for auth calls"
      pattern: "import.*createClient.*from.*@/lib/supabase/server"
    - from: "src/app/auth/confirm/route.ts"
      to: "src/lib/supabase/server.ts"
      via: "import createClient for OTP verification"
      pattern: "import.*createClient.*from.*@/lib/supabase/server"
    - from: "src/app/(auth)/reset-password/actions.ts"
      to: "src/lib/supabase/server.ts"
      via: "import createClient for password update"
      pattern: "import.*createClient.*from.*@/lib/supabase/server"
---

<objective>
Build the complete auth UI: login page with email/password form, password reset flow (request + update), email confirmation route handler, sign out action, and protected home page.

Purpose: Deliver the user-facing auth experience. After this plan, Pavel can sign in, reset his password, and sign out. All AUTH requirements are met.
Output: Working auth flow end-to-end — login, password reset, sign out, protected home page.
</objective>

<execution_context>
@C:/Users/fatpasha/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/fatpasha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@TECH_SPEC.md (Module 1: Auth & Session)
@CLAUDE.md

<interfaces>
<!-- From Plan 01 — Supabase utilities the executor will import -->

From src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>
// Usage: const supabase = await createClient()
// Context: Server Components, Server Actions, Route Handlers
```

From src/lib/supabase/client.ts:
```typescript
export function createClient(): SupabaseClient
// Usage: const supabase = createClient()
// Context: Client Components (use client)
```

shadcn/ui components available (from Plan 01):
- Card, CardContent, CardHeader, CardTitle from @/components/ui/card
- Input from @/components/ui/input
- Button from @/components/ui/button
- Label from @/components/ui/label
- Alert, AlertDescription from @/components/ui/alert
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create login page with Server Actions</name>
  <files>src/app/(auth)/login/page.tsx, src/app/(auth)/login/login-form.tsx, src/app/(auth)/login/actions.ts</files>
  <read_first>
    - C:/firstcladue/.planning/phases/01-foundation/01-CONTEXT.md (D-01: email+password only; D-03: minimal centered card; D-04: inline error below form; D-05: forgot password link)
    - C:/firstcladue/.planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: Login Server Action, Login Form Component code examples)
    - C:/firstcladue/src/lib/supabase/server.ts (createClient import path and signature)
    - C:/firstcladue/src/components/ui/card.tsx (available Card exports)
    - C:/firstcladue/src/components/ui/input.tsx (available Input exports)
    - C:/firstcladue/src/components/ui/button.tsx (available Button exports)
  </read_first>
  <action>
    1. Create `src/app/(auth)/login/actions.ts` with `'use server'` directive:

       **`login(formData: FormData)`:**
       - Extract `email = String(formData.get('email') ?? '').trim()` and `password = String(formData.get('password') ?? '')`
       - Validate: if `!email` return `{ error: 'Email is required' }`; if `!password` return `{ error: 'Password is required' }`
       - Call `const supabase = await createClient()` (import from `@/lib/supabase/server`)
       - Call `const { error } = await supabase.auth.signInWithPassword({ email, password })`
       - If error: return `{ error: 'Invalid email or password' }` (generic message per D-04, don't expose internal details)
       - If success: call `redirect('/')` (import from `next/navigation`)

       **`resetPassword(formData: FormData)`:**
       - Extract `email = String(formData.get('email') ?? '').trim()`
       - Validate: if `!email` return `{ error: 'Email is required' }`
       - Call `await supabase.auth.resetPasswordForEmail(email, { redirectTo: \`\${process.env.NEXT_PUBLIC_SITE_URL}/auth/confirm?next=/reset-password\` })`
       - If error: `console.error('[resetPassword] error:', error)` but do NOT reveal to user
       - Always return `{ success: true }` (security: don't reveal if email exists)

    2. Create `src/app/(auth)/login/login-form.tsx` as a `'use client'` component:
       - Import `useState` from react
       - Import `login`, `resetPassword` from `./actions`
       - Import shadcn/ui: `Button`, `Input`, `Label`, `Card`, `CardContent`, `CardHeader`, `CardTitle`
       - State: `error: string | null`, `loading: boolean`, `resetSent: boolean`, `showResetForm: boolean`
       - **Login form** (per D-03: minimal centered card):
         - `<Card className="w-full max-w-sm">`
         - `<CardHeader><CardTitle>Sign In</CardTitle></CardHeader>`
         - `<CardContent>` containing:
           - `<form>` with `action={handleLogin}` and `className="space-y-4"`
           - Email field: `<Label htmlFor="email">Email</Label>` + `<Input id="email" name="email" type="email" required />`
           - Password field: `<Label htmlFor="password">Password</Label>` + `<Input id="password" name="password" type="password" required />`
           - Inline error (per D-04): `{error && <p className="text-sm text-destructive">{error}</p>}` — displayed below the form fields, above the button
           - Submit button: `<Button type="submit" className="w-full" disabled={loading}>{loading ? 'Signing in...' : 'Sign In'}</Button>`
           - Forgot password link (per D-05): `<button type="button" onClick={() => setShowResetForm(true)} className="text-sm text-muted-foreground underline">Forgot password?</button>`
       - **Reset password form** (toggled by `showResetForm`):
         - Same Card, title "Reset Password"
         - Email field only
         - Submit calls `handleResetPassword` which calls `resetPassword(formData)`
         - On success (`resetSent`): show "Check your email for a reset link" message
         - "Back to login" link to toggle back
       - **handleLogin function**: `setError(null); setLoading(true); const result = await login(formData); if (result?.error) setError(result.error); setLoading(false);`
       - **handleResetPassword function**: `setError(null); const result = await resetPassword(formData); if (result?.success) setResetSent(true); if (result?.error) setError(result.error);`

    3. Create `src/app/(auth)/login/page.tsx` as a Server Component:
       - Import `LoginForm` from `./login-form`
       - Render: `<main className="flex min-h-screen items-center justify-center bg-background"><LoginForm /></main>`
       - This is the minimal centered layout per D-03.
  </action>
  <verify>
    <automated>cd C:/firstcladue && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(auth)/login/actions.ts` has `'use server'` directive: grep `use server` src/app/\(auth\)/login/actions.ts
    - `src/app/(auth)/login/actions.ts` exports login function: grep `export async function login` src/app/\(auth\)/login/actions.ts
    - `src/app/(auth)/login/actions.ts` exports resetPassword function: grep `export async function resetPassword` src/app/\(auth\)/login/actions.ts
    - `src/app/(auth)/login/actions.ts` uses signInWithPassword: grep `signInWithPassword` src/app/\(auth\)/login/actions.ts
    - `src/app/(auth)/login/actions.ts` returns generic error: grep `Invalid email or password` src/app/\(auth\)/login/actions.ts
    - `src/app/(auth)/login/actions.ts` uses resetPasswordForEmail: grep `resetPasswordForEmail` src/app/\(auth\)/login/actions.ts
    - `src/app/(auth)/login/actions.ts` imports createClient from @/lib/supabase/server: grep `@/lib/supabase/server` src/app/\(auth\)/login/actions.ts
    - `src/app/(auth)/login/login-form.tsx` has `'use client'` directive: grep `use client` src/app/\(auth\)/login/login-form.tsx
    - `src/app/(auth)/login/login-form.tsx` has inline error display: grep `text-destructive` src/app/\(auth\)/login/login-form.tsx
    - `src/app/(auth)/login/login-form.tsx` has forgot password UI: grep -i `forgot\|reset` src/app/\(auth\)/login/login-form.tsx
    - `src/app/(auth)/login/page.tsx` imports LoginForm: grep `LoginForm` src/app/\(auth\)/login/page.tsx
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Login page renders centered card with email+password form. Login Server Action validates input, calls Supabase signInWithPassword, returns generic error on failure, redirects on success. Password reset request action sends email without revealing if address exists. Inline error display below form per D-04. Forgot password link per D-05.</done>
</task>

<task type="auto">
  <name>Task 2: Create password reset completion, email confirm handler, sign out, and protected home page</name>
  <files>src/app/(auth)/reset-password/page.tsx, src/app/(auth)/reset-password/reset-password-form.tsx, src/app/(auth)/reset-password/actions.ts, src/app/auth/confirm/route.ts, src/app/page.tsx, src/app/actions.ts</files>
  <read_first>
    - C:/firstcladue/.planning/phases/01-foundation/01-RESEARCH.md (Pattern 5: Password Reset Update, Pattern 6: Email Confirm Handler, Sign Out Action code)
    - C:/firstcladue/.planning/phases/01-foundation/01-CONTEXT.md (D-02: password reset required; D-06: all routes protected)
    - C:/firstcladue/src/lib/supabase/server.ts (createClient import)
    - C:/firstcladue/src/app/(auth)/login/login-form.tsx (see established UI patterns from Task 1)
  </read_first>
  <action>
    1. Create `src/app/(auth)/reset-password/actions.ts` with `'use server'` directive:

       **`updatePassword(formData: FormData)`:**
       - Extract `password = String(formData.get('password') ?? '')` and `confirmPassword = String(formData.get('confirmPassword') ?? '')`
       - Validate: if `!password || password.length < 6` return `{ error: 'Password must be at least 6 characters' }`
       - Validate: if `password !== confirmPassword` return `{ error: 'Passwords do not match' }`
       - Call `const supabase = await createClient()` (import from `@/lib/supabase/server`)
       - Call `const { error } = await supabase.auth.updateUser({ password })`
       - If error: return `{ error: 'Failed to update password. Please try again.' }`
       - If success: call `redirect('/')` (import from `next/navigation`)

    2. Create `src/app/(auth)/reset-password/reset-password-form.tsx` as `'use client'` component:
       - Import `useState` from react
       - Import `updatePassword` from `./actions`
       - Import shadcn/ui: `Button`, `Input`, `Label`, `Card`, `CardContent`, `CardHeader`, `CardTitle`
       - State: `error: string | null`, `loading: boolean`
       - Card layout matching login page style (same max-w-sm, centered):
         - Title: "Set New Password"
         - Password field: `<Label htmlFor="password">New Password</Label>` + `<Input id="password" name="password" type="password" required minLength={6} />`
         - Confirm field: `<Label htmlFor="confirmPassword">Confirm Password</Label>` + `<Input id="confirmPassword" name="confirmPassword" type="password" required />`
         - Inline error: `{error && <p className="text-sm text-destructive">{error}</p>}`
         - Submit button: `<Button type="submit" className="w-full" disabled={loading}>{loading ? 'Updating...' : 'Update Password'}</Button>`
       - handleSubmit: call `updatePassword(formData)`, handle error response, set loading states

    3. Create `src/app/(auth)/reset-password/page.tsx` as Server Component:
       - Import `ResetPasswordForm` from `./reset-password-form`
       - Render: `<main className="flex min-h-screen items-center justify-center bg-background"><ResetPasswordForm /></main>`

    4. Create `src/app/auth/confirm/route.ts` — GET handler for email confirmation/reset links:
       - Import `EmailOtpType` from `@supabase/supabase-js`
       - Import `NextRequest` from `next/server`
       - Import `createClient` from `@/lib/supabase/server`
       - Import `redirect` from `next/navigation`
       - Export async function `GET(request: NextRequest)`:
         - Extract `token_hash` and `type` from `request.url` searchParams
         - Extract `next` param with fallback to `'/'`
         - If `token_hash && type`: call `supabase.auth.verifyOtp({ type: type as EmailOtpType, token_hash })`
         - If no error: `redirect(next)`
         - Otherwise: `redirect('/login?error=invalid-link')`

    5. Create `src/app/actions.ts` with `'use server'` directive:

       **`signOut()`:**
       - Call `const supabase = await createClient()` (import from `@/lib/supabase/server`)
       - Call `await supabase.auth.signOut()`
       - Call `redirect('/login')`

    6. Update `src/app/page.tsx` — protected home page:
       - Import `createClient` from `@/lib/supabase/server`
       - Import `redirect` from `next/navigation`
       - Import `signOut` from `./actions`
       - Import `Button` from `@/components/ui/button`
       - This is an async Server Component:
         - Call `const supabase = await createClient()`
         - Call `const { data: { user } } = await supabase.auth.getUser()`
         - If `!user`: `redirect('/login')` (defense in depth — middleware also protects)
         - Render minimal page: heading "Update Tracker", user's email displayed, sign out button (form with action={signOut})
       - This is a placeholder — Phase 2 will replace with the input form.
  </action>
  <verify>
    <automated>cd C:/firstcladue && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(auth)/reset-password/actions.ts` has `'use server'`: grep `use server` src/app/\(auth\)/reset-password/actions.ts
    - `src/app/(auth)/reset-password/actions.ts` exports updatePassword: grep `export async function updatePassword` src/app/\(auth\)/reset-password/actions.ts
    - `src/app/(auth)/reset-password/actions.ts` validates password length: grep `6` src/app/\(auth\)/reset-password/actions.ts
    - `src/app/(auth)/reset-password/actions.ts` validates password match: grep `do not match\|Passwords do not match` src/app/\(auth\)/reset-password/actions.ts
    - `src/app/(auth)/reset-password/actions.ts` uses updateUser: grep `updateUser` src/app/\(auth\)/reset-password/actions.ts
    - `src/app/(auth)/reset-password/reset-password-form.tsx` has `'use client'`: grep `use client` src/app/\(auth\)/reset-password/reset-password-form.tsx
    - `src/app/(auth)/reset-password/reset-password-form.tsx` has confirmPassword field: grep `confirmPassword` src/app/\(auth\)/reset-password/reset-password-form.tsx
    - `src/app/auth/confirm/route.ts` exports GET: grep `export async function GET` src/app/auth/confirm/route.ts
    - `src/app/auth/confirm/route.ts` uses verifyOtp: grep `verifyOtp` src/app/auth/confirm/route.ts
    - `src/app/auth/confirm/route.ts` redirects on invalid link: grep `invalid-link` src/app/auth/confirm/route.ts
    - `src/app/actions.ts` has `'use server'`: grep `use server` src/app/actions.ts
    - `src/app/actions.ts` exports signOut: grep `export async function signOut` src/app/actions.ts
    - `src/app/actions.ts` calls signOut on supabase: grep `supabase.auth.signOut` src/app/actions.ts
    - `src/app/page.tsx` uses getUser for auth check: grep `getUser` src/app/page.tsx
    - `src/app/page.tsx` has sign out form/button: grep `signOut` src/app/page.tsx
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Password reset flow complete: user can enter new password after clicking email link. Email confirmation route handler verifies OTP tokens. Sign out action clears session and redirects to login. Home page shows user email and sign out button, with server-side auth check as defense in depth.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete auth flow end-to-end</name>
  <files>none</files>
  <action>Human verification of the complete auth flow. No code changes — this is a manual testing checkpoint.</action>
  <what-built>Complete auth flow: login with email/password, password reset via email, sign out, route protection (redirects + API 401)</what-built>
  <how-to-verify>
    1. Ensure `.env.local` has your Supabase URL and anon key set
    2. Ensure your Supabase project has a user created (Dashboard -> Authentication -> Users)
    3. Run `npm run dev` — should start without errors
    4. Visit http://localhost:3000 — should redirect to /login
    5. See a centered card with "Sign In" title, email field, password field, submit button, "Forgot password?" link
    6. Enter wrong credentials — see inline error "Invalid email or password" below the form (not a toast)
    7. Enter correct credentials — get redirected to / (home page)
    8. Home page shows your email and a "Sign Out" button
    9. Refresh the page — session persists (still logged in, not redirected to login)
    10. Click "Sign Out" — redirected to /login
    11. Try accessing http://localhost:3000 directly — should redirect to /login (not authenticated)
    12. (Optional) Test "Forgot password?" link — should show reset form, submit sends email
  </how-to-verify>
  <verify>
    <automated>cd C:/firstcladue && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>User confirms: login works, session persists, sign out works, route protection works, password reset flow works.</done>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `npx tsc --noEmit` reports no type errors
3. Login page renders at /login with email+password form
4. Successful login redirects to /
5. Failed login shows inline error
6. Password reset flow sends email and allows new password entry
7. Sign out clears session and redirects to /login
8. Unauthenticated access to / redirects to /login
9. Session persists across browser refresh
</verification>

<success_criteria>
- AUTH-01: User can sign in with email and password (login form + Server Action)
- AUTH-02: Session persists across refresh (Supabase SSR cookies + middleware token refresh)
- AUTH-03: All pages redirect to /login when unauthenticated (middleware + server-side getUser check)
- D-01: Email + password only (no magic link option)
- D-02: Password reset via email link works
- D-03: Minimal centered card design
- D-04: Inline error below form
- D-05: "Forgot password?" link present
- D-06: API routes return 401 JSON (middleware handles)
- D-07: Only /login and reset flow pages are public
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
