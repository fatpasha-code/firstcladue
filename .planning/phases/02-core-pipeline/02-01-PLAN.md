---
phase: 02-core-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260325200000_create_records.sql
  - src/lib/ai/schemas.ts
  - src/lib/ai/extraction.ts
  - src/lib/ai/interpretation.ts
  - package.json
  - .env.example
  - vercel.json
autonomous: true
requirements: [INGEST-02, EXTRACT-01, EXTRACT-02, INTERP-01]

must_haves:
  truths:
    - "records table exists in Supabase with all columns from TECH_SPEC.md"
    - "RLS policies allow only the owning user to SELECT, INSERT, UPDATE"
    - "Zod schemas match extracted_data and interpretation JSON structures from TECH_SPEC.md"
    - "runExtraction returns typed ExtractedData with deadlines[].type: explicit|inferred"
    - "runInterpretation returns typed Interpretation with real_status: green|yellow|red"
    - "AI models are read from env vars, not hardcoded"
  artifacts:
    - path: "supabase/migrations/20260325200000_create_records.sql"
      provides: "records table with RLS and index"
      contains: "CREATE TABLE records"
    - path: "src/lib/ai/schemas.ts"
      provides: "Zod schemas and TypeScript types for extraction and interpretation"
      exports: ["ExtractedDataSchema", "InterpretationSchema", "ExtractedData", "Interpretation"]
    - path: "src/lib/ai/extraction.ts"
      provides: "Claude extraction pipeline function"
      exports: ["runExtraction"]
    - path: "src/lib/ai/interpretation.ts"
      provides: "Claude interpretation pipeline function"
      exports: ["runInterpretation"]
  key_links:
    - from: "src/lib/ai/extraction.ts"
      to: "src/lib/ai/schemas.ts"
      via: "import ExtractedDataSchema"
      pattern: "import.*ExtractedDataSchema.*from.*schemas"
    - from: "src/lib/ai/interpretation.ts"
      to: "src/lib/ai/schemas.ts"
      via: "import InterpretationSchema"
      pattern: "import.*InterpretationSchema.*from.*schemas"
---

<objective>
Database migration for the `records` table, install Anthropic SDK + Zod, and build the AI extraction/interpretation pipeline modules.

Purpose: Establish the data layer and AI pipeline that Plan 02 will wire into Server Actions and UI.
Output: Migration file, Zod schemas, `runExtraction()` and `runInterpretation()` functions, env vars configured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-pipeline/02-CONTEXT.md
@.planning/phases/02-core-pipeline/02-RESEARCH.md
@TECH_SPEC.md
@.claude/rules/database.md
@.claude/rules/api.md
@.claude/skills/create-migration.md

<interfaces>
<!-- Existing server client used by downstream Server Actions -->

From src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>
```

From src/app/actions.ts (existing):
```typescript
'use server'
export async function signOut(): Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, init Supabase, create records migration</name>
  <files>package.json, supabase/migrations/20260325200000_create_records.sql, vercel.json, .env.example</files>
  <read_first>
    - package.json (current dependencies)
    - .env.example (current env vars)
    - TECH_SPEC.md (records table schema -- "Database Schema" section)
    - .claude/rules/database.md (migration rules)
    - .claude/skills/create-migration.md (migration template)
    - .planning/phases/02-core-pipeline/02-RESEARCH.md (pitfalls 2, 5, 6 -- Supabase init, RLS INSERT policy)
  </read_first>
  <action>
1. Install dependencies with pinned versions (no ^ prefix per CLAUDE.md):
   ```bash
   npm install @anthropic-ai/sdk@0.80.0 zod@4.3.6
   ```

2. Install shadcn Textarea component:
   ```bash
   npx shadcn add textarea
   ```

3. Initialize Supabase local directory (supabase/ does not exist yet -- Pitfall 2 from RESEARCH.md):
   ```bash
   npx supabase init
   ```

4. Create migration file `supabase/migrations/20260325200000_create_records.sql` with this exact content:
   ```sql
   -- UP: Create records table for text analysis pipeline
   CREATE TABLE records (
     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
     user_id UUID NOT NULL REFERENCES auth.users(id),
     source_type TEXT DEFAULT 'text',
     raw_text TEXT NOT NULL,
     label TEXT,
     status TEXT DEFAULT 'pending' CHECK (status IN ('pending', 'processing', 'completed', 'failed')),
     extracted_data JSONB,
     user_corrections JSONB,
     interpretation JSONB,
     error_message TEXT,
     reviewed_at TIMESTAMPTZ,
     created_at TIMESTAMPTZ DEFAULT now(),
     updated_at TIMESTAMPTZ DEFAULT now()
   );

   ALTER TABLE records ENABLE ROW LEVEL SECURITY;

   CREATE POLICY "own_records_select" ON records
     FOR SELECT USING (auth.uid() = user_id);
   CREATE POLICY "own_records_insert" ON records
     FOR INSERT WITH CHECK (auth.uid() = user_id);
   CREATE POLICY "own_records_update" ON records
     FOR UPDATE USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id);

   CREATE INDEX idx_records_user_created ON records(user_id, created_at DESC);

   -- DOWN: DROP TABLE IF EXISTS records;
   ```

   Note: Uses separate RLS policies for SELECT/INSERT/UPDATE per Pitfall 6 from RESEARCH.md. INSERT requires WITH CHECK (not USING).

5. Create `vercel.json` in project root to avoid Server Action timeout (Pitfall 1 from RESEARCH.md):
   ```json
   {
     "functions": {
       "app/**/*": {
         "maxDuration": 60
       }
     }
   }
   ```

6. Update `.env.example` -- append these lines:
   ```bash
   # Anthropic AI
   ANTHROPIC_API_KEY=your_anthropic_api_key
   CLAUDE_EXTRACTION_MODEL=claude-haiku-4-5
   CLAUDE_INTERPRETATION_MODEL=claude-sonnet-4-5
   ```
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const ok = p.dependencies['@anthropic-ai/sdk'] === '0.80.0' && p.dependencies['zod'] === '4.3.6'; console.log(ok ? 'PASS' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"@anthropic-ai/sdk": "0.80.0"` (no ^)
    - package.json contains `"zod": "4.3.6"` (no ^)
    - File `supabase/migrations/20260325200000_create_records.sql` exists
    - Migration contains `CREATE TABLE records`
    - Migration contains `ALTER TABLE records ENABLE ROW LEVEL SECURITY`
    - Migration contains `CREATE POLICY "own_records_insert"` with `WITH CHECK`
    - Migration contains `CREATE INDEX idx_records_user_created`
    - Migration contains DOWN comment: `DROP TABLE IF EXISTS records`
    - File `vercel.json` exists with `"maxDuration": 60`
    - `.env.example` contains `ANTHROPIC_API_KEY`
    - `.env.example` contains `CLAUDE_EXTRACTION_MODEL`
    - `.env.example` contains `CLAUDE_INTERPRETATION_MODEL`
    - `src/components/ui/textarea.tsx` exists (shadcn textarea installed)
  </acceptance_criteria>
  <done>Dependencies installed, Supabase initialized, records migration created with proper RLS, vercel.json timeout configured, env vars documented.</done>
</task>

<task type="auto">
  <name>Task 2: Create Zod schemas and AI pipeline modules</name>
  <files>src/lib/ai/schemas.ts, src/lib/ai/extraction.ts, src/lib/ai/interpretation.ts</files>
  <read_first>
    - TECH_SPEC.md (extracted_data JSON structure in "Module 2a: Extraction", interpretation JSON structure in "Module 2b: Interpretation")
    - .planning/phases/02-core-pipeline/02-RESEARCH.md (Pattern 2: Anthropic SDK Structured Outputs, Zod schema examples, Pitfall 5: Russian output)
    - .planning/phases/02-core-pipeline/02-CONTEXT.md (D-03: Russian output requirement)
    - .claude/rules/api.md (Claude API calls: official SDK, retry, no logging user data)
  </read_first>
  <action>
1. Create directory `src/lib/ai/`.

2. Create `src/lib/ai/schemas.ts` with Zod schemas matching TECH_SPEC.md JSON structures exactly:
   ```typescript
   import { z } from 'zod'

   export const ExtractedDataSchema = z.object({
     done: z.array(z.object({
       description: z.string(),
       person: z.string().optional()
     })),
     in_progress: z.array(z.object({
       description: z.string(),
       person: z.string().optional(),
       deadline: z.string().optional()
     })),
     blockers: z.array(z.object({
       description: z.string(),
       impact: z.enum(['high', 'medium', 'low'])
     })),
     assignments: z.array(z.object({
       person: z.string(),
       tasks: z.array(z.string()),
       by_when: z.string().optional()
     })),
     deadlines: z.array(z.object({
       date: z.string(),
       description: z.string(),
       type: z.enum(['explicit', 'inferred'])
     }))
   })

   export const InterpretationSchema = z.object({
     summary: z.string(),
     management_view: z.string(),
     hidden_blockers: z.array(z.string()),
     ambiguities: z.array(z.string()),
     clarification_questions: z.array(z.string()),
     real_status: z.enum(['green', 'yellow', 'red'])
   })

   export type ExtractedData = z.infer<typeof ExtractedDataSchema>
   export type Interpretation = z.infer<typeof InterpretationSchema>
   ```

3. Create `src/lib/ai/extraction.ts`:
   - Import `Anthropic` from `@anthropic-ai/sdk`
   - Import `zodOutputFormat` from `@anthropic-ai/sdk/helpers/zod`
   - Import `ExtractedDataSchema` from `./schemas`
   - Create `const client = new Anthropic()` (SDK reads ANTHROPIC_API_KEY from env automatically)
   - Export `async function runExtraction(rawText: string): Promise<ExtractedData>`
   - Read model from `process.env.CLAUDE_EXTRACTION_MODEL` -- throw Error if not set: `throw new Error('CLAUDE_EXTRACTION_MODEL env var is not set')`
   - Call `client.messages.parse()` with:
     - `model`: from env var
     - `max_tokens`: 4096
     - `system`: Russian-only extraction prompt (per D-03): `"Ты -- аналитик, извлекающий структурированные данные из текстов рабочих созвонов и переписок. Весь вывод строго на русском языке. Все поля, описания и значения -- только на русском.\n\nИзвлеки из текста:\n- done: что завершено\n- in_progress: что в работе\n- blockers: блокеры и их уровень влияния (high/medium/low)\n- assignments: кто за что отвечает и к какому сроку\n- deadlines: явные (explicit) и выведенные из контекста (inferred) дедлайны\n\nЕсли информации нет для какой-то категории, верни пустой массив."`
     - `messages`: `[{ role: 'user', content: rawText }]`
     - `output_config`: `{ format: zodOutputFormat(ExtractedDataSchema) }`
   - Return `response.parsed_output`
   - Wrap in try/catch, log error to console (`console.error('[runExtraction] error:', error)`) and re-throw

4. Create `src/lib/ai/interpretation.ts`:
   - Same imports pattern as extraction.ts
   - Import `InterpretationSchema`, `ExtractedData` from `./schemas`
   - Export `async function runInterpretation(rawText: string, extractedData: ExtractedData): Promise<Interpretation>`
   - Read model from `process.env.CLAUDE_INTERPRETATION_MODEL` -- throw Error if not set
   - Call `client.messages.parse()` with:
     - `model`: from env var
     - `max_tokens`: 4096
     - `system`: Russian-only interpretation prompt (per D-03): `"Ты -- управленческий аналитик. Твоя задача -- интерпретировать технические обновления на языке менеджера.\n\nВесь вывод строго на русском языке. Все поля, описания и значения -- только на русском.\n\nНа основе исходного текста и извлечённых данных определи:\n- summary: краткое резюме ситуации (2-3 предложения)\n- management_view: что это значит для менеджера (риски, здоровье задач)\n- hidden_blockers: скрытые блокеры которые не названы явно\n- ambiguities: неясности и расплывчатые формулировки\n- clarification_questions: конкретные вопросы которые стоит задать разработчику\n- real_status: green (всё по плану), yellow (есть вопросы/риски), red (критические проблемы)\n\nЕсли скрытых блокеров или неясностей нет, верни пустые массивы."`
     - `messages`: `[{ role: 'user', content: "Исходный текст:\n\n" + rawText + "\n\nИзвлечённые данные:\n\n" + JSON.stringify(extractedData, null, 2) }]`
     - `output_config`: `{ format: zodOutputFormat(InterpretationSchema) }`
   - Return `response.parsed_output`
   - Same try/catch + console.error pattern, re-throw
  </action>
  <verify>
    <automated>npx tsc --noEmit src/lib/ai/schemas.ts src/lib/ai/extraction.ts src/lib/ai/interpretation.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/ai/schemas.ts` exists and exports `ExtractedDataSchema`, `InterpretationSchema`, `ExtractedData`, `Interpretation`
    - `ExtractedDataSchema` contains `deadlines` array with `type: z.enum(['explicit', 'inferred'])` (per EXTRACT-02)
    - `InterpretationSchema` contains `real_status: z.enum(['green', 'yellow', 'red'])`
    - File `src/lib/ai/extraction.ts` exports `runExtraction`
    - `extraction.ts` reads model from `process.env.CLAUDE_EXTRACTION_MODEL` (not hardcoded)
    - `extraction.ts` contains `zodOutputFormat(ExtractedDataSchema)` call
    - `extraction.ts` system prompt contains Russian-only instruction: "Весь вывод строго на русском языке"
    - File `src/lib/ai/interpretation.ts` exports `runInterpretation`
    - `interpretation.ts` reads model from `process.env.CLAUDE_INTERPRETATION_MODEL`
    - `interpretation.ts` contains `zodOutputFormat(InterpretationSchema)` call
    - `interpretation.ts` system prompt contains "Весь вывод строго на русском языке"
    - Both extraction.ts and interpretation.ts have try/catch with `console.error`
    - Neither file logs user data content (only error objects)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Zod schemas match TECH_SPEC.md JSON structures. runExtraction and runInterpretation use Anthropic SDK structured outputs with zodOutputFormat. Models from env vars. Russian output enforced in system prompts. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds (no TypeScript errors)
- `supabase/migrations/20260325200000_create_records.sql` contains correct schema
- `src/lib/ai/schemas.ts` exports match TECH_SPEC.md JSON structures
- `src/lib/ai/extraction.ts` and `interpretation.ts` use `messages.parse()` with `zodOutputFormat()`
- No hardcoded model IDs in any AI file
- `.env.example` lists all 3 new env vars
</verification>

<success_criteria>
- records migration ready for application to Supabase
- AI pipeline modules compile and export correct functions
- Zod schemas enforce extracted_data and interpretation JSON contracts
- Inferred deadlines are typed as `'explicit' | 'inferred'` (EXTRACT-02)
- All env vars documented in .env.example
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pipeline/02-01-SUMMARY.md`
</output>
